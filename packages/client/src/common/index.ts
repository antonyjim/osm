/**
 * Right now we are just trying to export all modules
 */

export namespace OSM.Common {
  export class ClientConfig {
    /**
     *
     */
    constructor() {
      console.log('ClientConfig Called')
    }
  }
}
