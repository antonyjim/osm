;`
Provide a single list item for a single domain
`

import * as React from 'react'

interface IDomainRecordListItem {
  domainName: string
}

export default function DomainRecordListItem(
  domainListProps: IDomainRecordListItem
) {}
