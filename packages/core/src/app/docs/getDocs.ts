;`
Return & format documentation information
`

export function getSingleDoc(conditions: { id?: string; title?: string }) {
  return new Promise((resolveDocItem, rejectDocItem) => {})
}

export function getDocList(conditions: {
  module?: string
  hot?: boolean
  searchPhrase?: string
}) {
  return new Promise((resolveDocList, rejectDocList) => {})
}
