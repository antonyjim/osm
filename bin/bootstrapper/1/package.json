{
  "name": "{{shortName}}",
  "version": "1.0.0",
  "description": "{{description}}",
  "scripts": {
    "test": "npm run test"
  },
  "author": "Anthony Jund <antonyjund@gmail.com>",
  "license": "ISC",
  "OSM": {
    "shortName": "{{shortName}}",
    "longName": "{{longName}}",
    "basePath": "{{baseClientPath}}",
    "baseApiPath": "{{baseApiPath}}",
    "client": "core",
    "lib": "shared",
    "entry": {
      "proxy": "{{proxyArgs}}",
      "spawn": "{{spawnArgs}}"
    }
  },
  "dependencies": {
    "@types/react": "^16.9.9",
    "@types/react-router-dom": "^5.1.0"
  }
}